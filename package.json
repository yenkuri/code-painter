{
  "name": "code-painter",
  "version": "0.1.0",
  "private": true,
  "description": "Schema-first architecture mapping tool for JSON diagrams rendered by a frontend grid/canvas system.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}

